services:
  app:
    build: .
    ports:
      - "80:80"
    # Credentials are baked into the image too, but reading .env here means editing it
    # takes effect on `docker compose up` without rebuilding.
    env_file: .env
